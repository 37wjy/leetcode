#include <iostream>
#include <string>
#include <cstdio>
#include <algorithm>

#define MAXN 5000

using namespace std;

char a[MAXN + 10], b[MAXN + 10];
int na, nb;

int solve() {
    return 233;
}

int main() {
    scanf("%d%s", &na, a + 1);
    scanf("%d%s", &nb, b + 1);
    printf("%d\n", solve());
    return 0;
}
